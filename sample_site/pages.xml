<?xml version="1.0" encoding="UTF-8" ?>
<site>
	{% set all_docs = model.get('/test/_all_docs') %}
	{% set meta = model.post(
		api = '/test',
		data = {
			'type': 'temp',
			'created': time.strftime('%Y-%m-%d %H:%M:%S')
		}
	) %}


	<page>
		<url>/</url>
		<context>{{ dict(docs = all_docs, meta = meta) }}</context>
		<template>home.html</template>
	</page>


	{% for row in all_docs['rows'] %}
	<page>
		<url>/doc/{{ row['id'] }}/</url>
		<context>
			{{
				model.get(
					api = '/test/' + row['id'],
					immutable = True
				)
			}}
		</context>
		<template>document.html</template>
	</page>
	{% endfor %}
</site>
